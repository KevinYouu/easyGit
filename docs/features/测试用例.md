# 测试用例文档

## 功能说明

本文档记录 easyGit 项目的测试用例实现,包括各个模块的测试覆盖率和测试策略。

## 实现位置

测试文件分布在各个包的 `*_test.go` 文件中:

- `internal/gitcmd/branch_test.go` - 分支操作测试
- `internal/gitcmd/merge_test.go` - 合并操作测试
- `internal/gitcmd/reset_test.go` - 重置操作测试
- `internal/gitcmd/tag_test.go` - 标签操作测试
- `internal/gitcmd/stats_test.go` - 统计功能测试
- `internal/form/form_test.go` - 表单组件逻辑测试(输入验证、默认值)
- `internal/form/helpbar_test.go` - 统一帮助栏渲染测试(徽章、单行不折行、超宽截断、4 行终端隐藏、OptionLabel 单行组装)
- `internal/form/render_test.go` - 表单基础组件渲染测试(Select/MultiSelect/TableSelect/TableMultiSelect + 共享辅助函数)
- `internal/form/render_command_test.go` - 命令表单渲染测试(驱动生产构造器, 按命令 i18n 键组织用例)
- `internal/form/render_input_confirm_test.go` - 输入框/确认框渲染测试(驱动生产构造器, 标题按命令实际拼装)
- `internal/command/progress_model_test.go` - 进度条模型测试(含 View 渲染)
- `internal/config/config_test.go` - 配置管理测试
- `internal/i18n/i18n_test.go` - 国际化测试
- `internal/testutil/git_helper.go` - 测试辅助工具

## 测试覆盖率

当前测试覆盖率(按包):

| 包名 | 覆盖率 | 说明 |
|------|--------|------|
| colors | 75.0% | 颜色渲染功能 |
| command | 26.8% | 命令执行框架(含进度条 View 渲染) |
| config | 58.1% | 配置管理 |
| **form** | **54.4%** | **表单组件(渲染级测试驱动生产表单构造器)** |
| gitcmd | 20.5% | Git 命令封装 |
| i18n | 93.0% | 国际化 |
| logs | 100.0% | 日志输出 |
| random | 100.0% | 随机数生成 |
| spinner | 14.2% | 加载动画(交互式) |
| theme | 27.6% | 主题样式 |
| update | 9.8% | 自动更新 |
| version | 50.0% | 版本信息 |

## 运行测试

### 运行所有测试

```bash
go test ./...
```

### 查看测试覆盖率

```bash
go test ./... -cover
```

### 运行特定包的测试

```bash
go test ./internal/gitcmd -v
```

### 生成覆盖率报告

```bash
go test ./... -coverprofile=coverage.out
go tool cover -html=coverage.out
```

## 测试用例详解

### 1. gitcmd 包测试

#### branch_test.go

测试分支相关功能:

- `TestGetCurrentBranch` - 获取当前分支
- `TestGetCurrentBranch_NotGitRepo` - 非 Git 仓库错误处理
- `TestGetAllBranches` - 获取所有分支
- `TestGetAllBranches_EmptyRepo` - 空仓库处理
- `TestGetAllBranches_NotGitRepo` - 非 Git 仓库错误处理

**实现位置**: `internal/gitcmd/branch_test.go:11-114`

#### merge_test.go

测试合并相关功能:

- `TestGetCurrentBranch_Merge` - 合并时获取当前分支
- `TestGetLocalBranches` - 获取本地分支列表
- `TestGetLocalBranches_OnlyCurrentBranch` - 只有当前分支的情况
- `TestCheckWorkingDirectoryStatus_Clean` - 工作目录干净状态检查
- `TestMergeStrategy_DefaultValues` - 合并策略配置验证
- `TestGetRemoteBranches_NoRemote` - 无远程仓库处理

**实现位置**: `internal/gitcmd/merge_test.go:12-144`

#### reset_test.go

测试重置相关功能:

- `TestCommit_Structure` - Commit 结构体验证
- `TestGetModeDescription` - 重置模式描述(含 default 空值模式)
- `TestResetModeOptions` - 重置模式 4 项单选选项:顺序与值语义(""/--soft/--mixed/--hard)、Label/Description 分字段、OptionLabel 单行组装无换行
- `TestCommit_TimestampParsing` - 时间戳解析
- `TestCommit_MessageTruncation` - 提交消息截断

**实现位置**: `internal/gitcmd/reset_test.go:8-180`

### 2. form 包测试

#### form_test.go

测试表单组件的逻辑部分:

- `TestInput_Validation` - 输入验证逻辑
- `TestConfirm_DefaultValue` - 确认框默认值
- `TestInput_DefaultValue` - 输入框默认值
- `TestInput_EmptyString` - 空字符串验证
- `TestInput_ValidString` - 有效字符串验证
- `TestConfirm_BooleanValue` - 布尔值处理

**实现位置**: `internal/form/form_test.go:8-102`

#### render_test.go(基础组件渲染)

通过生产表单构造器(`NewSelectForm`/`NewMultiSelectForm`/`NewTableSelectModel`/`NewTableMultiSelectModel`)+ `tea.WindowSizeMsg` 驱动渲染,验证高度自适应与内容驱动布局:

- `TestSelectRenderUsesAllSpace` - 单选列表按内容计算高度
- `TestSelectRenderManyOptions` - 大量选项时高度封顶
- `TestMultiSelectRenderUsesAllSpace` - 多选列表高度自适应
- `TestTableSelectRender` - 单选表格渲染
- `TestTableMultiSelectRender` - 多选表格渲染

共享辅助函数: `renderSelectField`、`renderMultiSelectField`、`visibleLabels`、`assertTableView`、`tableSizes`(8 种终端尺寸)、`commitLabels`、`testOptions`。

#### render_command_test.go(命令表单渲染)

测试与生产命令共用同一构造路径:渲染直接驱动生产构造器(`NewSelectForm`/`NewMultiSelectForm`/`NewTableMultiSelectModel`), 标题取命令实际 i18n 键, 选项按命令实际格式构造(装饰/本地化), 数据库与时间戳等动态内容为代表性取值:

- `TestCommandSelectRender` - 11 个命令的单选表单(含 merge/rebase 目标分支列表), 高度 `[24,12,10,8,6]`
- `TestCommandMultiSelectRender` - 4 个命令的多选表单(含 remote 远程仓库列表)
- `TestCommandTableRender` - drop/squash 多选表格(两个命令的实际 i18n 标题)、reset 提交列表
- `TestTableListLongMessageSingleLine` - 列表超长省略: 验证使用仓库 `SafeTruncate` 的 `...` 而非 bubbles 回退的 `…`, 覆盖单选/多选两种模型

选项单行说明用例(SelectForm 统一 `OptionLabel(名称, 说明)` 组装):

- reset 模式: 4 项含 default(不传参数),单行带说明 → SelectForm 用例(自表格分支移出)
- merge 策略: 两段式单行(name + Description,取代原 "name - description" 拼接),行数断言不变
- cherry-pick 选项 / rebase 动作 / 语言选项: name + desc 键单行
- push 提交类型: 代表性取值 + `commit.type.desc.<value>` 说明

关键断言 `assertCommandField`: 总高度 = `min(选项数+2, 终端高度)`(≥6 行终端,内容 1 + 选项 + 帮助 1)、`min(选项数+1, 终端高度)`(<6 行),行不越界。

#### render_input_confirm_test.go(输入/确认渲染)

同样驱动生产构造器(`NewInputForm`/`NewConfirmForm`);标题按命令实际 i18n 键与 `fmt.Sprintf` 格式拼装(与命令渲染完全一致):

- `TestCommandInputRender` - 5 个命令的输入框(push-all 默认 `fix: `、push-selected `feat: `、squash 旧提交消息、tag 版本号自增、tag 提交消息)
- `TestCommandConfirmRender` - 6 个命令的确认框(分支删除、分支远程删除、drop `5 个提交`、merge continue、reset 两行含 hard 警告、tag 删除两行)

关键断言 `assertCompactField`: 标题行存在、总高度不超过终端、无尾部空行、无宽度溢出。

**实现位置**: `internal/form/render_test.go`、`internal/form/render_command_test.go`、`internal/form/render_input_confirm_test.go`

> 说明: 生产表单构造器(`NewSelectForm`/`NewMultiSelectForm`/`NewInputForm`/`NewConfirmForm`/`NewTableMultiSelectModel`)同时被命令入口(`SelectForm` 等)与渲染测试调用, 表单配置(主题/帮助/过滤/高度公式)回归会直接导致渲染测试失败。

### 3. command 包测试

#### progress_model_test.go

进度条模型渲染测试:

- 初始状态: 步骤描述 + `(0/3)` + 3 个未完成圆点
- 进度条宽度自适应: 宽度 `[80,50,40,30,24]` 下进度条 ≤ 40 列且所有行不越界
- 状态图标切换: ✓/▶/⚡ 与 ✓×3/✅/❌
- `TestProgressHelpBar` - 执行中 ≥6 行终端渲染单行帮助栏;4 行终端隐藏;完成态保留 `ui.exiting.*` 提示不渲染帮助栏
- `TestProgressI18nStatus` - 初始状态文本取 `progress.preparing` i18n 键

**实现位置**: `internal/command/progress_model_test.go`

### 4. testutil 辅助工具

提供测试辅助函数:

- `CreateTempGitRepo(t)` - 创建临时 Git 仓库
- `RunGitCommand(t, dir, args...)` - 执行 Git 命令
- `SetupTestRepo(t)` - 设置测试仓库
- `CreateTestFile(t, repoDir, filename, content)` - 创建测试文件
- `CommitFile(t, repoDir, filename, message)` - 提交文件
- `CreateBranch(t, repoDir, branchName)` - 创建分支
- `CheckoutBranch(t, repoDir, branchName)` - 切换分支
- `CreateTag(t, repoDir, tagName)` - 创建标签

**实现位置**: `internal/testutil/git_helper.go`

## 测试策略

### 单元测试原则

1. **隔离性** - 每个测试独立运行,使用临时目录
2. **可重复性** - 测试结果稳定,不依赖外部状态
3. **清晰性** - 测试名称明确表达测试意图
4. **完整性** - 覆盖正常情况和边界情况

### Git 操作测试

对于 Git 操作测试:

1. 使用 `testutil.CreateTempGitRepo()` 创建临时仓库
2. 在测试结束后自动清理临时文件
3. 配置测试用户信息避免 Git 配置依赖
4. 测试正常流程和错误处理

### 交互式组件测试

对于交互式 UI 组件(form, progress):

1. 渲染测试驱动生产表单构造器(`NewSelectForm`/`NewMultiSelectForm`/`NewInputForm`/`NewConfirmForm`/`NewTableMultiSelectModel`), 与命令实际执行的构造路径完全一致, 防止表单配置回归
2. 用例按命令实际调用点组织: 标题取命令实际 i18n 键(Input/Confirm 动态消息按命令的 `fmt.Sprintf` 拼装), 选项/默认值为代表性数据
3. 覆盖多种终端尺寸(24 行到 60 行、24 列到 300 列), 验证高度自适应与不越界
4. 进度条使用 `NewProgressModelWithoutSpinner` 保证图标确定性
5. UI 逻辑(动画时序等)通过手动测试

渲染级测试要点:

- `tea.View.Content` 获取纯文本渲染结果
- 高度断言用行数, 宽度断言用 `ansi.Strip` 去除 ANSI 后按 rune 计数(进度条 `░`/`█` 为 3 字节 UTF-8)
- 下拉列表高度公式 `min(选项数+2, 终端高度)`(≥6 行,含帮助栏 1 行)、`min(选项数+1, 终端高度)`(<6 行帮助栏隐藏), 表格高度 `max(高度-3, 3)` 且不超过选项数(单选另预留帮助栏 1 行)
- Input/Confirm 无显式高度, 内容高度固定, 任何终端高度都满足
- 帮助栏可见性阈值 `HelpBarMinTermHeight` = 6:渲染测试覆盖 6 行边界与 4 行隐藏

## 配置

### 测试环境变量

无需特殊环境变量。

### 依赖工具

- Go 1.21+
- Git (用于 gitcmd 测试)

## 示例

### 运行测试并查看详细输出

```bash
go test ./internal/gitcmd -v
```

### 只运行特定测试

```bash
go test ./internal/gitcmd -run TestGetCurrentBranch
```

### 查看测试覆盖率详情

```bash
go test ./internal/gitcmd -cover -coverprofile=coverage.out
go tool cover -func=coverage.out
```

## 注意事项

1. **测试隔离** - 每个测试使用独立的临时 Git 仓库
2. **清理资源** - 使用 `t.TempDir()` 自动清理临时文件
3. **错误处理** - 测试同时验证成功和失败场景
4. **跨平台** - 测试代码在 Linux/macOS/Windows 上均可运行
5. **Git 版本** - 某些测试依赖 Git 2.28+ (主要是 init.defaultBranch 配置)

## 持续改进

### 待提升的测试覆盖率

1. **update 包** (当前 9.8%) - 自动更新功能
2. **gitcmd 包** (当前 20.5%) - Git 命令封装
3. **spinner 包** (当前 14.2%) - 加载动画
4. **theme 包** (当前 27.6%) - 主题样式

### 测试改进方向

1. 增加集成测试覆盖真实使用场景
2. 为 command 包的命令执行流程添加测试(表单提交后到 Git 调用)
3. 添加性能测试和压力测试
4. 增加边界条件和异常情况测试

## 相关文档

- [Go Testing 官方文档](https://golang.org/pkg/testing/)
- [Go 代码覆盖率工具](https://go.dev/blog/cover)
- 项目编码规范: `CLAUDE.md`
